{% extends 'ZeteqMarketBundle:Admin:admin_layout.html.twig' %}

{% block content -%}
    <h1>ShippingMethod list</h1>

    <table class="table table-hover table-responsive table-striped records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Cost</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('shippingmethod_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.name }}</td>
                <td>{{ entity.cost }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('shippingmethod_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('shippingmethod_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('shippingmethod_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
