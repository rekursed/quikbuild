{% extends 'ZeteqMarketBundle:Admin:admin_layout.html.twig' %}
{% block title %}
Store  --Main Admin
{% endblock %}
{% block content -%}

   




    <h1>Store</h1>

       <nav class="navbar navbar-default" role="navigation">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#nav_collapse">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
 
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div id="nav_collapse" class="collapse navbar-collapse">
            
            
                 <form method="post" enctype="multipart/form-data"  action="{{path('store_read_excel')}}"  class="navbar-form navbar-left">
            <div class="form-group">
              <input type="file" name="uploadedfile" class="">
            </div>
            <button class="btn btn-default" type="submit">Submit</button>
          </form>
            
            
                       <a  class="btn btn-info navbar-btn"  href="{{ path('store_new') }}">
                           <i class="fa fa-plus"></i>
                           Create a new Store
        </a>
        </div><!-- /.navbar-collapse -->
      </nav>
    
    
    
    
    
<div class="well">
{{ grid_search(grid, 'ZeteqMarketBundle:Admin:blocks.html.twig') }}
</div>
    
{{ grid(grid, 'ZeteqMarketBundle:Admin:blocks.html.twig') }}
 {% block grid_filters %}{% endblock %}
    {% endblock %}
