{% extends 'ZeteqAdminBundle:Default:admin_layout.html.twig' %}

{% block content -%}


  <div class="">
      <nav role="navigation" class="navbar navbar-default">
        <!-- Brand and toggle get grouped for better mobile display -->
        <div class="navbar-header">
          <button data-target="#bs-example-navbar-collapse-1" data-toggle="collapse" class="navbar-toggle" type="button">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a href="#" class="navbar-brand">ProductImage</a>
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <div  class="collapse navbar-collapse">

          <ul class="nav navbar-nav navbar-right">
            <li>            
            <a href="{{ path('productimage_new') }}">
                Create a new entry
            </a>
</li>
          </ul>
        </div><!-- /.navbar-collapse -->
      </nav>
    </div>


 

  <table class="table table-striped table-hover table-bordered records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Description</th>
                <th>Image_path</th>
                <th>Image</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        {% for entity in pagination %}
            <tr>
                <td><a href="{{ path('productimage_edit', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.name }}</td>
                <td>{{ entity.description }}</td>
                <td>{{ entity.imagepath }}</td>
                <td>{{ entity.image }}</td>
                <td>
                  <div class="btn-group pull-right">
                   
                        <a class="btn btn-default" href="{{ path('productimage_show', { 'id': entity.id }) }}">show</a>
                   
                        <a class="btn btn-default" href="{{ path('productimage_edit', { 'id': entity.id }) }}">edit</a>
                </div>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

<div class="navigation">
   {{ knp_pagination_render(pagination) }}
</div>

     
    {% endblock %}
